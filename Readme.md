This image runs Exchange online powershell on top of linux powershell core default image

To run this image execute:

docker run -it r00ts/exchangeonline

after connect to the container run the following command to connect to your exchange online instance

Connect-ExchangeOnline -Device